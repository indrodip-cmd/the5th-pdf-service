services:
  - type: web
    name: the5th-pdf-service
    runtime: node
    buildCommand: bash render-build.sh
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
